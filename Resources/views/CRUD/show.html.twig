{% extends 'LibrinfoCoreBundle:CRUD:base_show.html.twig' %}

{% block navbar_title %}
    {% if object.isStrain == true %}
        {{ 'Strain'|trans() }}: {{ admin.toString(object)|truncate(15) }}
    {% else %}
        {{ admin.label|trans() }}: {{ admin.toString(object)|truncate(15) }}
    {% endif %}
{% endblock %}